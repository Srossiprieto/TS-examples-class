/* function mostrarLongitud(objeto:number | string) {
    if(typeof objeto == "string"){
        return objeto.length
    }
    return objeto.toString().length
}

mostrarLongitud(1)

 */

/* interface Mario {
    company: 'nintendo',
    nombre: string,
    saltar: () => void
}


interface Sonic{
    company: 'sega',
    nombre: string,
    correr: () => void,
}

type Personaje = Mario | Sonic;

function jugar(personaje: Personaje){
    // console.log(personaje.)

    if(personaje.company === 'nintendo'){ // nos aseguramos con un if si es uno o otro.
        personaje.saltar() // <--- ESTE ES MARIO!
        return;
    }

    personaje.correr() // <--- solo llega sonic



}
 */





